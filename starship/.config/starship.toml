[directory]
truncation_length = 8
truncate_to_repo = true
style = "bold blue"
repo_root_style = "underline italic blue"
use_logical_path = false

[git_status]
format = '([\[$conflicted$deleted$renamed$modified$staged\]]($style) )'
ignore_submodules = true
